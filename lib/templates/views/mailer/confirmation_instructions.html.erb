<p><%= t(:'devise.mailer.confirmations.title', :email => @resource.email) %></p>

<p><%= t(:'devise.mailer.confirmations.description') %></p>

<p><%= link_to t(:'devise.mailer.confirmations.link'), confirmation_url(@resource, :confirmation_token => @resource.confirmation_token) %></p>
