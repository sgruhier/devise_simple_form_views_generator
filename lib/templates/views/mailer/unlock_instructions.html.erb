<p><%= t(:'devise.mailer.unlock.title', :email => @resource.email) %></p>

<p><%= t(:'devise.mailer.unlock.description') %></p>

<p><%= link_to t(:'devise.mailer.unlock.link'), confirmation_url(@resource, :confirmation_token => @resource.confirmation_token) %></p>
